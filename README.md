# 🧭 Internet Speed Test API

Simple FastAPI-based backend to simulate upload/download speed tests.

## 🚀 Run Locally

```bash
git clone https://github.com/YOUR_USERNAME/speedtest-api.git
cd speedtest-api
docker build -t speedtest-api .
docker run -p 8000:10000 speedtest-api
